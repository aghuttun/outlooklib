/// Binary entry point for local manual runs.
///
/// The primary functionality of this project is exposed through the library
/// crate and Python extension module.
fn main() {
    println!("outlooklib: Rust library crate");
}
